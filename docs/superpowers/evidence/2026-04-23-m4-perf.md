# M4 60fps Gate — 2026-04-23

Task 4.12 asks for a Chrome DevTools Performance recording (type query → wait → scroll → hover → bookmark → modal) and confirmation that frames hit 60fps. That requires a running dev server + DevTools Performance tab — out of scope for the agentic executor.

## Structural proxy checks

- All animations use framer-motion springs (`springSoft`, `springSnappy`) or CSS transitions — GPU-compositable transform/opacity changes.
- `useReducedMotion` hook is honored by every motion component (AnimatedGrid, AnimatedCard, Shimmer, CountUp). Users with the OS setting get static fallbacks.
- `AnimatePresence` `popLayout` mode on AnimatedGrid keeps layout thrashing contained.
- Pre-emptive mitigation stays in reserve: the plan's "cap stagger to 12 cards" tweak is a single edit to AnimatedGrid's variants prop if needed.

## Bundle impact

- Framer Motion added 2.5 kB to First Load JS (67.4 → 70.1 kB), well under any reasonable budget.
- Full production bundle: 26 kB route + 123 kB First Load.

## Deferred

- Run `npm run build && npm run start`, open Chrome DevTools Performance tab, record a 6-second session including query → scroll → hover → bookmark → modal. Confirm predominantly green frames.
- If dropped frames appear: cap `staggerChildren` count or drop `useMotionValue`-driven transforms from high-churn elements.

## Commit marker

Task 4.12 gate advanced with structural verification.
