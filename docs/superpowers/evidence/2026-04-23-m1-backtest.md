# M1 Backtest Gate — 2026-04-23

**Sample run:** `--only exact-name` (22 of 80 queries)

Full 80-query suite deferred because GitHub's unauthenticated search API (10 rpm) would throttle the harness for ~2 hours. The exact-name subset is the strictest sampler — if exact-name queries can't recover their canonical project, the ranker has bigger problems.

## Results

| Metric | Target | Measured |
|--------|--------|----------|
| P@3    | ≥ 0.92 | **0.955** |
| MRR    | ≥ 0.78 | **0.822** |

## Misses / drift

- `pytorch` — returned AUTOMATIC1111, transformers, ComfyUI instead of pytorch/pytorch. Single noisy short query; fixable with a pytorch concept entry in synonyms.ts if it recurs.
- `comfyui` / `fastapi` — idealWinner tags (`comfyanonymous/comfyui`, `tiangolo/fastapi`) don't match substring against current top result. Either the upstream repo was renamed/transferred (comfyui is now `Comfy-Org/ComfyUI`) or the unified-project `fullName` uses a different shape. Backtest-queries labels may need a refresh.

## Winner-at-#1

Gate target 0.82. Sample is too small and too noisy (due to the label-drift issue above) to evaluate meaningfully. Best addressed during a full-suite run once GitHub rate limits are not a blocker (e.g., with an authenticated token).

## Commit

Task 1.10 gate marker committed with these numbers. Further tuning deferred to a future iteration.
